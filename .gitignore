minerd*
cpuminer
*.exe
*.o
*.a
*.d
gmon.out

autom4te.cache
.deps

Makefile
Makefile.in
INSTALL
configure.lineno
depcomp
missing
install-sh
stamp-h1
cpuminer-config.h*
compile
config.log
config.status
config.status.lineno
config.guess
config.sub

mingw32-config.cache

.dirstamp
*~
*.iml

*.vcxproj.user
*.opensdf
*.sdf
*.suo
Release/
Debug/
x64/Release/
x64/Debug/
*.pdb/

installer/
res/cpuminer.aps
res/RC*
sign/
sign.sh

compat/curl-for-windows/

# generated per link, carries the SW built on date (see Makefile.am)
build-stamp.c

# `make check` artefacts: the KAT binary plus automake's test harness output.
# Listed by name rather than as *.log/*.trs so nothing else gets hidden.
randomx-kat
test-driver
test-suite.log
randomx-kat.log
randomx-kat.trs

# rename maps written by algo/randomx/prefix-syms.sh (also in CLEANFILES)
*.a.syms
